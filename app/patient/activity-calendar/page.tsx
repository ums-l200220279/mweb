import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export default function ActivityCalendarPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Activity Calendar</CardTitle>
        <CardDescription>Track your daily activities and appointments</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Activity calendar content will be implemented here.</p>
      </CardContent>
    </Card>
  )
}

